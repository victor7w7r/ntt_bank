info:
  name: Reports
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/customers/reports/1725082786?startDate=2022-02-08&endDate=2022-02-10
  params:
    - name: startDate
      value: 2022-02-08
      type: query
    - name: endDate
      value: 2022-02-10
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "test(\"Status code is 200\", function () {\r

        \    expect(res.getStatus()).to.equal(200);\r

        });\r

        test(\"Response is an array\", function() {\r

        \   expect(res.getBody()).to.be.an(\"array\");\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
